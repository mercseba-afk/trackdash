# R3 final notes

The R3 market model is now the canonical Price Intelligence architecture. Legacy completed-sale-only estimates remain for audit/backward compatibility, but current public starting prices use only purchasable R3 offer signals. Provider activation is intentionally separate from code/schema rollout.
